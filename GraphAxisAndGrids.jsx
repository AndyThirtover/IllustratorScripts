////     Graph Axis and Grids//// Constantsvar width = 3840;var height = 2160;var x_origin = 100;var y_origin = 100;var x_span = 3600;var y_span = 2000;var graph_type = 'linear';//gridvar minorStroke = 1;var minorColor = makeColor(128,128,128);var majorStroke = 5;var majorColor = makeColor(32,32,32);//X axisvar x_steps = 10;var x_start = 0;var x_multiplier = 10;var x_ticks = 5;var x_angle = 0;var x_axis_text_offset = 60;//Y axisvar y_steps = 10;var y_start = 0;var y_multiplier = 10;var y_ticks = 10;var y_angle = 90;var y_axis_text_offset = 20;var doc = app.documents.add(null,width,height);function makeColor(r,g,b){    var c = new RGBColor();    c.red   = r;    c.green = g;    c.blue  = b;    return c;}function put_value(value,x,y,angle){    this_text = doc.textFrames.pointText([x,y]);    this_text.contents = value.toString();    this_text.paragraphs[0].paragraphAttributes.justification = Justification.CENTER;    this_text.rotate(angle);    var fontStyle = this_text.textRange.characterAttributes;	fontStyle.size = 48;}function place_grid(x,y,xe,ye,width,color) {    var this_path = doc.pathItems.add();    this_path.setEntirePath ([[x,y],[xe,ye]]);    this_path.strokeWidth = width;    this_path.strokeColor = color;}// Write X valuesvar step;for (step = 0; step <= x_steps; step++) {    put_value(    	x_start + (step*x_multiplier),    	x_origin + (step*x_span/x_steps),    	y_origin - x_axis_text_offset,    	x_angle);}// Write Y valuesfor (step = 0; step <= y_steps; step++) {    put_value(    	y_start + (step*y_multiplier),    	x_origin - y_axis_text_offset,    	y_origin + (step*y_span/y_steps),    	y_angle);}// Add the Minor Griddoc.layers.add().name = "MinorGrid" ;// X linesminsteps = x_steps * x_ticks ;for (step = 0; step <= minsteps; step++){	place_grid(		x_origin + (step*x_span/minsteps),		y_origin,		x_origin + (step*x_span/minsteps),		y_origin + y_span,		minorStroke,		minorColor		);}minsteps = y_steps * y_ticks ;for (step = 0; step <= minsteps; step++){	place_grid(		x_origin,		y_origin + (step*y_span/minsteps),		x_origin + x_span,		y_origin + (step*y_span/minsteps),		minorStroke,		minorColor		);}// Add the Major Griddoc.layers.add().name = "MajorGrid" ;// X linesminsteps = x_steps;for (step = 0; step <= minsteps; step++){	place_grid(		x_origin + (step*x_span/minsteps),		y_origin,		x_origin + (step*x_span/minsteps),		y_origin + y_span,		majorStroke,		majorColor		);}minsteps = y_steps;for (step = 0; step <= minsteps; step++){	place_grid(		x_origin,		y_origin + (step*y_span/minsteps),		x_origin + x_span,		y_origin + (step*y_span/minsteps),		majorStroke,		majorColor		);}a = Math.log(200);